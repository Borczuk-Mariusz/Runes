/**
 * SelectRunePrompt displays the default prompt when no rune is selected.
 */
export default function SelectRunePrompt() {
  return (
    <div style={{ color: "#d4af37", fontSize: "16px", padding: "4px 0" }}>
      Click a rune to see its symbolic color
    </div>
  );
}
